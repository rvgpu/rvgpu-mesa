
#include "rc_bld_ir_common.h"
#include "rc_build_type.h"

void rc_exec_mask_init(struct rc_exec_mask *mask, struct rc_build_context *bld) {
    //mask->bld = bld;
   // mask->exec_mask = LLVMConstAllOnes();
}